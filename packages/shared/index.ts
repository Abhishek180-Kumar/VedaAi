// Shared types and utilities for VedaAI packages.
// Currently the web app keeps its domain types in apps/web/types,
// but this file serves as the canonical shared export point for future
// cross-package usage.

export {};
